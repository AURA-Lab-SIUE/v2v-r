% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reliability.R
\name{reliability}
\alias{reliability}
\title{Compute intercoder reliability between two coders}
\usage{
reliability(coder_a, coder_b, method = c("kappa", "alpha"))
}
\arguments{
\item{coder_a}{Vector of codes from the first coder.}

\item{coder_b}{Vector of codes from the second coder. Same length and
levels as \code{coder_a}.}

\item{method}{One of \code{"kappa"} (default, Cohen's) or \code{"alpha"}
(Krippendorff's, for any level of measurement).}
}
\value{
A list with elements \code{statistic} (the numeric value),
\code{method} (the method name), \code{interpretation} (Landis & Koch label),
and \code{raw} (the full underlying object from \code{irr} for further
inspection).
}
\description{
Wraps \code{\link[irr:kappa2]{irr::kappa2()}} (Cohen's kappa, for two coders and nominal codes)
and \code{\link[irr:kripp.alpha]{irr::kripp.alpha()}} (Krippendorff's alpha, which extends to more
than two coders and to ordinal/interval/ratio codes). Returns a list
with the chosen statistic and a Landis & Koch (1977) interpretation
label so students can read the result without consulting a separate
reference table.
}
\details{
Landis & Koch benchmarks: \verb{< 0.20} = slight, \code{0.21-0.40} = fair,
\code{0.41-0.60} = moderate, \code{0.61-0.80} = substantial, \code{0.81-1.00} =
almost perfect. The V2V graduate threshold is \verb{>= 0.70}; values below
that mean the codebook needs revision, not that the coders need to
work harder.
}
\examples{
library(v2v)

# Synthetic example: two coders mostly agree on whether messages contain emotes
msgs <- twitch_chat(channels = "bobross", n = 50)
coder_a <- grepl("LULW|Pog|KEKW", msgs$message)
coder_b <- grepl("LULW|Pog|KEKW|Kappa", msgs$message) # slightly different rule

reliability(coder_a, coder_b, method = "kappa")
}
