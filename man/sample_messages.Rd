% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample.R
\name{sample_messages}
\alias{sample_messages}
\title{Draw a stratified sample of chat messages by channel}
\usage{
sample_messages(chat, n_per_channel = 100, seed = 20261113)
}
\arguments{
\item{chat}{A tibble with a \code{channel} column (typically the output of
\code{\link[=twitch_chat]{twitch_chat()}}).}

\item{n_per_channel}{Integer number of rows to draw per channel.
Default \code{100} is calibrated for in-class coding exercises.}

\item{seed}{Integer RNG seed. Default \code{20261113} matches the seed used
to build the shipped fixture, so the same call returns the same
sample across runs.}
}
\value{
A tibble with the same columns as \code{chat}, with up to
\code{n_per_channel * dplyr::n_distinct(chat$channel)} rows.
}
\description{
Returns a balanced sample with \code{n_per_channel} rows from each distinct
value of \code{chat$channel}. Channels with fewer than \code{n_per_channel} rows
contribute all of their rows. Chapter 10 introduces this function as
the pedagogically correct alternative to a naive
\code{dplyr::slice_sample(n)}, which produces biased coverage when channels
have unequal volume.
}
\examples{
library(v2v)

# Default: 100 messages from each of the 8 focal channels
sample_messages(twitch_chat())

# Larger per-channel draw for a reliability exercise
sample_messages(twitch_chat(), n_per_channel = 250)
}
