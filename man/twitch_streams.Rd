% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loaders.R
\name{twitch_streams}
\alias{twitch_streams}
\title{Load Twitch stream snapshots}
\usage{
twitch_streams(channels = NULL, games = NULL)
}
\arguments{
\item{channels}{Optional character vector of channel names. Exact,
case-sensitive match against the bare channel name.}

\item{games}{Optional character vector of game (Twitch category)
names. Exact, case-sensitive match. NULL games are excluded when
\code{games} is non-NULL.}
}
\value{
A tibble with the same six columns as \link{twitch_streams_sample}.
}
\description{
Returns the bundled \link{twitch_streams_sample} tibble with optional
filtering. Same usage shape as \code{\link[=twitch_chat]{twitch_chat()}}.
}
\examples{
library(v2v)

# Full sample
streams <- twitch_streams()

# Bob Ross's Art-category snapshots
streams <- twitch_streams(channels = "bobross", games = "Art")

# All non-gaming snapshots across the focal set
streams <- twitch_streams(games = c("Just Chatting", "Art"))
}
