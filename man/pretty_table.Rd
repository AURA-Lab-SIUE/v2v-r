% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{pretty_table}
\alias{pretty_table}
\title{Format a data frame as a tidy HTML/PDF table}
\usage{
pretty_table(x, caption = NULL, digits = 2, ...)
}
\arguments{
\item{x}{A data frame or tibble.}

\item{caption}{Optional table caption string. Default \code{NULL} (no caption).}

\item{digits}{Number of decimal places for numeric columns. Default \code{2}.}

\item{...}{Additional arguments passed to \code{\link[knitr:kable]{knitr::kable()}}.}
}
\value{
A \code{knitr_kable} object, rendered as a table in Quarto.
}
\description{
A thin wrapper around \code{\link[knitr:kable]{knitr::kable()}} that applies consistent styling for
the V2V Quarto book. Accepts a data frame or tibble (typically the output of
a \code{dplyr::summarise()} chain) and returns a formatted table that renders
correctly in both the HTML and PDF profiles.
}
\examples{
library(dplyr)
mtcars |>
  group_by(cyl) |>
  summarise(mean_mpg = mean(mpg), n = n()) |>
  pretty_table(caption = "Miles per gallon by cylinder count")

}
