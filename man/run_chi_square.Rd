% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{run_chi_square}
\alias{run_chi_square}
\title{Chi-square test of independence with Cramer's V}
\usage{
run_chi_square(data, x, y)
}
\arguments{
\item{data}{A data frame.}

\item{x}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Unquoted name of the
first categorical variable.}

\item{y}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Unquoted name of the
second categorical variable.}
}
\value{
A list (returned invisibly) with elements \code{chi_sq}, \code{df},
\code{p_value}, \code{cramers_v}, \code{interpretation}, and \code{raw}.
}
\description{
Wraps \code{\link[stats:chisq.test]{stats::chisq.test()}} and computes Cramer's V as the effect-size
measure. Prints a student-legible summary and returns the full results
invisibly. Introduced in Chapter 13.
}
\details{
Cramer's V benchmarks (Cohen, 1988): 0.1 = small, 0.3 = medium, 0.5 = large.
}
\examples{
library(v2v)
library(dplyr)

streams <- twitch_streams() |>
  filter(!is.na(game)) |>
  mutate(
    is_gaming   = !(game \%in\% c("Art", "Just Chatting", "Music")),
    viewer_band = cut(viewers, breaks = c(0, 5000, 20000, Inf),
                      labels = c("small", "medium", "large"))
  )

run_chi_square(streams, x = is_gaming, y = viewer_band)
}
