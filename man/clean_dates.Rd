% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dates.R
\name{clean_dates}
\alias{clean_dates}
\title{Convert Twitch millisecond timestamps to POSIXct}
\usage{
clean_dates(x, tz = "UTC")
}
\arguments{
\item{x}{Numeric vector of Unix-epoch values in milliseconds.}

\item{tz}{Time zone for the returned POSIXct. Defaults to \code{"UTC"} because
the source data is timezone-naive UTC. Pass \code{"America/Chicago"} (or
another Olson name) to convert.}
}
\value{
A POSIXct vector the same length as \code{x}.
}
\description{
The \code{date} column in both \link{twitch_chat_sample} and \link{twitch_streams_sample}
is stored as bigint Unix epoch in \strong{milliseconds}, not seconds. A naive
\code{as.POSIXct(date, origin = "1970-01-01")} produces timestamps in the year
50,888 and visibly nonsensical plots. This wrapper performs the divide-by-1000
step alongside the conversion so the failure mode cannot happen by accident.
}
\details{
This is the headline conversion lesson of Chapter 11. The function exists
to be teachable, not to hide the arithmetic: see \code{body(clean_dates)}.
}
\examples{
library(v2v)

chat <- twitch_chat(channels = "bobross", n = 5)
clean_dates(chat$date)
clean_dates(chat$date, tz = "America/Chicago")

# The wrong way (do NOT do this):
as.POSIXct(chat$date, origin = "1970-01-01") # year 50,888
}
