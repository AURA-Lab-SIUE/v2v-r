% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deploy.R
\name{deploy_portfolio}
\alias{deploy_portfolio}
\title{Deploy a student portfolio to GitHub Pages}
\usage{
deploy_portfolio(path = ".", render = TRUE, ask = interactive())
}
\arguments{
\item{path}{Path to the Quarto project root. Defaults to the current working
directory.}

\item{render}{Logical. If \code{TRUE} (default), run \verb{quarto render} before
publishing. Set to \code{FALSE} to publish a previously rendered site without
re-rendering.}

\item{ask}{Logical. If \code{TRUE} (default in interactive sessions), prompt for
confirmation before publishing. Set to \code{FALSE} for non-interactive use.}
}
\value{
Invisibly returns \code{TRUE} if the deploy succeeded, \code{FALSE} if a
pre-flight check failed (no error is thrown; the specific failure is
reported to the console).
}
\description{
Runs a three-step pre-flight checklist before publishing: verifies that the
\code{renv} library is synchronized, that there are no uncommitted changes in the
working tree, and that a \verb{_quarto.yml} file exists in the project root. If
all checks pass, the function renders the Quarto project and publishes it to
GitHub Pages using \verb{quarto publish}.
}
\details{
This function is designed to be called from the root of a student Quarto
portfolio project (the directory that contains \verb{_quarto.yml}).
}
\examples{
\dontrun{
# From your portfolio project root:
v2v::deploy_portfolio()
}

}
